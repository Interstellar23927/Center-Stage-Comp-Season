/*package org.firstinspires.ftc.teamcode.TeleOp;
import com.qualcomm.robotcore.hardware.Gamepad;
import org.firstinspires.ftc.teamcode.HardwareRobot;


public class Slides {
    HardwareRobot robot;
    public Slides(HardwareRobot r) {
        robot = r;
    }


 public void Update(Gamepad gamepad1, Gamepad gamepad2) {

        //change buttons

        if (gamepad2.dpad_up) {
            robot.slideRight.setPower(0.5);
            robot.slideLeft.setPower(0.5);
        }
        robot.slideRight.setPower(0.0);
        robot.slideLeft.setPower(0.0);

        if (gamepad2.dpad_down) {
            robot.slideRight.setPower(-0.2);
            robot.slideLeft.setPower(-0.2);
        }

        robot.slideRight.setPower(0.0);
        robot.slideLeft.setPower(0.0);
        }
    }*/




